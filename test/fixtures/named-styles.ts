import { css } from 'ecij';

export const accentColor = 'crimson';
export const accentSize = 14;

export const accentClass = css`
  /* accent */
  background: ${accentColor};
  font-size: ${accentSize}px;
`;
